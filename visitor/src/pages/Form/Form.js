import React, { PureComponent } from 'react';
import { Button } from 'antd-mobile';

class Form extends PureComponent {
  render() {
    return <Button type="primary">FormPages</Button>
  }
}

export default Form;